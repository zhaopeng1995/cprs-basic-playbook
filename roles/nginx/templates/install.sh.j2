#!/bin/bash
cd /tmp/nginx-{{nginx_version}}

./configure --prefix={{nginx_basedir}} --sbin-path={{nginx_basedir}}/sbin/nginx --with-http_ssl_module --with-http_flv_module --with-http_gzip_static_module   --with-http_stub_status_module

make && make install

